__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

.pytest_cache/
.coverage

# Render output and tuning artefacts -- regenerate them, do not commit.
preview.png
palette_card.png
dither_lab*.png

# Never commit the API key.
.env
birddisplay.env

# The frame's wifi credentials. secrets.example.py is the template.
firmware/inky_frame/secrets.py

# Exported boards. The workflow publishes these to the `board` branch;
# a local run should not leave them in the working tree.
board.png
board.json
